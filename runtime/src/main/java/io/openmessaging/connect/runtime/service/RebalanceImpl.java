package io.openmessaging.connect.runtime.impl;

import io.openmessaging.connect.runtime.Worker;

public class RebalanceImpl {

    private final Worker worker;
    public RebalanceImpl(Worker worker) {
        this.worker = worker;
    }
}
